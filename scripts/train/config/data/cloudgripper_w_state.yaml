dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 1
  name: cloudgripper.lance
  keys_to_load:
    - pixels
    - action
    - state
  keys_to_cache:
    - action
    - state
